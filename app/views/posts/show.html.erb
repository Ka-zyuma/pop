<div class="memo_main">
  <div class="memo_show_main">
    <%= form_tag("/posts/#{@post.id}/update",) do %>
      <textarea name="content"><%= @post.content %></textarea>
      <br>
      <div class="memo_show_botton">
        <input type="submit" value="保存">
      </div>
    <% end %>
    <div class="memo_show_botton">
      <%=button_to "削除" , "/posts/#{@post.id}/delete" %>
    </div>
  </div>
</div>
